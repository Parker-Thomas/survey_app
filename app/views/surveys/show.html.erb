<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @survey.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @survey.description %>
</p>

<p>
  <strong>Author:</strong>
  <%= @survey.author.name %>
</p>

<% @survey.questions.each do |q| %>
  <%= q.text %> <br>
  <%= q.description %> <br>
  <%= render 'form_for_response', question_id: q.id %>
<% end %>

<%= link_to 'Edit', edit_survey_path(@survey) %> |
<%= link_to 'Back', surveys_path %>
